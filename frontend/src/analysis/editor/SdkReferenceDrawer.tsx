import { sdkManifest } from '@logbook/analysis-sdk';
export function SdkReferenceDrawer() { return <details className="analysis-panel"><summary>SDK reference</summary><p>Constructors: {sdkManifest.constructors.join(', ')}</p><p><strong>Point mapping:</strong> <code>{'event.values().mapPoints((point) => point.withValue((point.value ?? 0) * 2))'}</code></p><p><strong>Reusable functions:</strong> call published or session functions through the typed namespace, for example <code>udf.normalize(...)</code>.</p><ul>{sdkManifest.operations.map((operation) => <li key={`${operation.inputType}.${operation.methodName}`}><code>{operation.inputType}.{operation.methodName}()</code><span>{operation.description}</span></li>)}</ul></details>; }
