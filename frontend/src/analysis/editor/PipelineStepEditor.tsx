import { pipelineOperationRegistry, type PipelineOperationDescriptor } from '@logbook/analysis-sdk';
import type { AnalysisFunctionSummary, PipelineDefinitionV1 } from '../../types';
export function PipelineStepEditor({ step, index, functions, onChange, onRemove }: { step: PipelineDefinitionV1['steps'][number]; index: number; functions: AnalysisFunctionSummary[]; onChange: (step: PipelineDefinitionV1['steps'][number]) => void; onRemove: () => void }) {
  const descriptor = pipelineOperationRegistry.find((item) => item.methodName === step.operation || item.aliases?.includes(step.operation)) as PipelineOperationDescriptor | undefined;
  const compatible = functions.filter((item) => item.published_revision_id && (!descriptor?.compatibleFunctionKinds || descriptor.compatibleFunctionKinds.includes(item.function_kind)));
  return <div className="pipeline-step"><span className="pipeline-step-index">{index + 1}</span><select value={step.operation} onChange={(event) => onChange({ operation: event.target.value })}>{[...new Map(pipelineOperationRegistry.map((item) => [item.methodName, item])).values()].map((item) => <option key={item.methodName} value={item.methodName}>{item.editor.label}</option>)}</select>{descriptor?.compatibleFunctionKinds?.length ? <select value={step.functionBinding ?? ''} onChange={(event) => { const functionBinding = event.target.value; onChange({ ...step, ...(functionBinding ? { functionBinding } : {}) }); }}><option value="">Select function</option>{compatible.map((item) => <option key={item.id} value={item.function_key}>{item.name}</option>)}</select> : null}{step.operation === 'transformWindow' && <label>Window<input type="number" min={1} value={step.windowSize ?? 4} onChange={(event) => onChange({ ...step, windowSize: Number(event.target.value) })} /></label>}<button type="button" className="danger" onClick={onRemove}>Remove</button></div>;
}
